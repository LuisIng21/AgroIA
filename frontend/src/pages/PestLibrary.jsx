import React from 'react';
import { Box, Typography } from '@mui/material';

const PestLibrary = () => (
  <Box>
    <Typography variant="h4">Biblioteca de Plagas</Typography>
    <Typography variant="body1" sx={{ mt: 2 }}>
      Catálogo completo de plagas y enfermedades del frijol.
    </Typography>
  </Box>
);

export default PestLibrary;